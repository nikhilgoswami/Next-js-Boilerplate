// @flow
// import { all } from 'redux-saga/effects';

export default function* rootSaga(): Generator<any, void, unknown> {
  // any is for yealded values | void is for return type | unknown is for arguments
  // yield all([layoutSaga()]);
}
